# Rust toolchain

# Requirements
Rust and Cargo should ne installed with
```
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```
Latest recommendations can be found at https://www.rust-lang.org/tools/install